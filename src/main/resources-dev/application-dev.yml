spring:
    config:
        activate:
            on-profile: dev
    data:
        redis:
            port: 6379
            host: localhost
    jpa:
        hibernate:
            ddl-auto: update
server:
    port: 8080